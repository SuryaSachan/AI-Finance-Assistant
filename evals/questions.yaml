# Accuracy benchmark for the Finance Assistant.
#
# Each case pairs a natural-language question with ground-truth SQL that is run
# directly against DuckDB. The assistant passes only if the number it reports
# matches the ground truth (and, where stated, if it picked the right dataset /
# intent / filters).
#
# Placeholders available in expect_sql:
#   {anchor} {lm_start} {lm_end} {pm_start} {pm_end} {ytd_start} {year}

- id: q01
  question: How much did we pay out last month?
  dataset: transactions
  intent: aggregate
  must_filter: [transaction_type=debit]
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'debit'
      AND transaction_date BETWEEN DATE '{lm_start}' AND DATE '{lm_end}'

- id: q02
  question: Which transactions are still unreconciled?
  dataset: transactions
  intent: list
  must_filter: [reconciliation_status=unreconciled]
  check: count
  expect_sql: >
    SELECT count(*) FROM v_transactions WHERE reconciliation_status = 'unreconciled'

- id: q03
  question: What was our total spend last month?
  session: s1
  dataset: transactions
  intent: aggregate
  must_filter: [transaction_type=debit]
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'debit'
      AND transaction_date BETWEEN DATE '{lm_start}' AND DATE '{lm_end}'

- id: q04
  question: How does that compare to the month before?
  session: s1
  after: q03
  dataset: transactions
  check: comparison_previous
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'debit'
      AND transaction_date BETWEEN DATE '{pm_start}' AND DATE '{pm_end}'

- id: q05
  question: Show me the top 5 counterparties by spend this year
  dataset: transactions
  intent: aggregate
  group_by: [counterparty]
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'debit'
      AND transaction_date BETWEEN DATE '{ytd_start}' AND DATE '{anchor}'

- id: q06
  question: How much money did we receive last month?
  dataset: transactions
  must_filter: [transaction_type=credit]
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'credit'
      AND transaction_date BETWEEN DATE '{lm_start}' AND DATE '{lm_end}'

- id: q07
  question: How much did we spend over UPI last month?
  dataset: transactions
  must_filter: [channel=UPI, transaction_type=debit]
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'debit' AND channel = 'UPI'
      AND transaction_date BETWEEN DATE '{lm_start}' AND DATE '{lm_end}'

- id: q08
  question: What is the total value of unreconciled transactions last month?
  dataset: transactions
  must_filter: [reconciliation_status=unreconciled]
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE reconciliation_status = 'unreconciled'
      AND transaction_date BETWEEN DATE '{lm_start}' AND DATE '{lm_end}'

- id: q09
  question: Break down spend by bank last month
  dataset: transactions
  intent: aggregate
  group_by: [bank_name]
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'debit'
      AND transaction_date BETWEEN DATE '{lm_start}' AND DATE '{lm_end}'

- id: q10
  question: Show NEFT spend by month year to date
  dataset: transactions
  intent: trend
  must_filter: [channel=NEFT]
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'debit' AND channel = 'NEFT'
      AND transaction_date BETWEEN DATE '{ytd_start}' AND DATE '{anchor}'

- id: q11
  question: How much did we pay TATA CAPITAL LIMITED last month?
  dataset: transactions
  must_filter: [counterparty=TATA CAPITAL LIMITED]
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'debit' AND counterparty = 'TATA CAPITAL LIMITED'
      AND transaction_date BETWEEN DATE '{lm_start}' AND DATE '{lm_end}'

- id: q12
  question: How many transactions did we have last month?
  dataset: transactions
  check: count
  expect_sql: >
    SELECT count(*) FROM v_transactions
    WHERE transaction_date BETWEEN DATE '{lm_start}' AND DATE '{lm_end}'

- id: q13
  question: What was the average transaction amount last month?
  dataset: transactions
  check: value
  expect_sql: >
    SELECT avg(amount) FROM v_transactions
    WHERE transaction_date BETWEEN DATE '{lm_start}' AND DATE '{lm_end}'

- id: q14
  question: What is the total balance across HDFC accounts?
  dataset: accounts
  must_filter: [bank_code=HDFC]
  check: value
  expect_sql: >
    SELECT sum(available_balance) FROM v_accounts WHERE bank_code = 'HDFC'

- id: q15
  question: How much did we pay in bank charges this year?
  dataset: transactions
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'debit' AND channel = 'CHARGES'
      AND transaction_date BETWEEN DATE '{ytd_start}' AND DATE '{anchor}'

- id: q16
  question: Which counterparties did we pay the most last month?
  dataset: transactions
  group_by: [counterparty]
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'debit'
      AND transaction_date BETWEEN DATE '{lm_start}' AND DATE '{lm_end}'

- id: q17
  question: Break down spend by channel this year
  dataset: transactions
  group_by: [channel]
  check: value
  expect_sql: >
    SELECT sum(amount) FROM v_transactions
    WHERE transaction_type = 'debit'
      AND transaction_date BETWEEN DATE '{ytd_start}' AND DATE '{anchor}'

- id: q18
  question: How much did we spend with Globex Corporation last month?
  expect_refusal: true
  note: Globex Corporation appears in no narration; the assistant must refuse.

- id: q19
  question: What will our spend be next quarter?
  expect_refusal: true
  note: Forecasting is outside the dataset; the assistant must not invent a number.

- id: q20
  question: How many unreconciled transactions were there last month?
  dataset: transactions
  must_filter: [reconciliation_status=unreconciled]
  check: count
  expect_sql: >
    SELECT count(*) FROM v_transactions
    WHERE reconciliation_status = 'unreconciled'
      AND transaction_date BETWEEN DATE '{lm_start}' AND DATE '{lm_end}'
